pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
matplotlib>=3.6
seaborn>=0.12
scipy>=1.9
jupyter>=1.0

# Optional: install separately to reproduce the original article's exact best model.
# Not required to run the notebook -- a scikit-learn GradientBoostingClassifier
# substitute is used automatically if xgboost is unavailable.
# xgboost>=1.6
